import { createSlice } from '@reduxjs/toolkit'
import type { PayloadAction } from '@reduxjs/toolkit'




export const AuthSlice = createSlice({
    name: "Auth",
    initialState: {
        currentUser : null as any
    },
 reducers:{ loginstart: (state) => {
   
  },}
})